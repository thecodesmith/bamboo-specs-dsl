language: groovy

jdk:
  - oraclejdk8

after_success:
  - ./gradlew jacocoTestReport coveralls

# Required for Gradle caching
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
